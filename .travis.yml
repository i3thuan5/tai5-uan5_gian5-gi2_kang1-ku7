dist: jammy
branches:
  only:
  - master
jobs:
  include:
    - name: ruby
      language: ruby
      jdk: openjdk17
      script:
        - java --version
    - name: python
      language: python
      python: '3.10'
      jdk: openjdk17
      script:
        - java --version
