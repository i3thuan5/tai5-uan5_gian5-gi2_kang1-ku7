dist: jammy
branches:
  only:
  - master
jobs:
  include:
    - name: ruby
      language: ruby
      jdk: openjdk17
      script:
        - java --version
    - name: python
      language: python
      jdk: openjdk17
      script:
        - java --version